
def main():
    # args

    # Set up data dir path and load the study configs
   
    # Create PipelineObjects

    # For each object, Run the functions to create the pipeline (or import data).

   '''
    Notes:
    PipelineObject is created. Has attributes and methods defined for it's specific
    database and structure.

    Access `import` and `sql creation` with a specific database class.
    Access `dbt files` with a specific structure class.
    Access functions used across the board with a single class.

    Q: Does this seem likely to work?
    Q: Does this seem ~easy to modify in the future?
    Q: Naming convention for baseclass files?

    
    scripts to p_utils or something
    get rid of models

    data classes
    '''

